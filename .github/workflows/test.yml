name: Tests
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm ci
      - run: npm test
      - name: Boot test
        run: timeout 5 node index.js || [ $? -eq 124 ]
        env:
          SUPABASE_URL: http://localhost:0
          SUPABASE_KEY: test
